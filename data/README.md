# Data

This folder holds sample/simulated data used by the traffic light simulator, or a documented reference to the data source if the data is generated at runtime.

_Update this file to describe:_
- Where simulation data comes from (e.g., randomly generated vehicle counts, a public traffic dataset, sensor simulation logic)
- Any external dataset name/link, if used
- File formats used (CSV, JSON, etc.)
